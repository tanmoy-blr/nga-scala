package com.nag.repository

case class SensorStatDto(
                          count : Int,
                          sum : Int,
                          min : Int,
                          max: Int
                        )
